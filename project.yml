name: BENDREffects
options:
  bundleIdPrefix: com.bendr.fxplug
  deploymentTarget:
    macOS: "13.0"
  xcodeVersion: "16.0"
  createIntermediateGroups: true
  defaultConfig: Release

settings:
  base:
    MACOSX_DEPLOYMENT_TARGET: "13.0"
    SWIFT_VERSION: "5.9"
    # FxPlug framework headers and runtime
    FRAMEWORK_SEARCH_PATHS:
      - "$(SRCROOT)/Frameworks"
      - '"/Applications/Final Cut Pro.app/Contents/Frameworks"'
      - "$(inherited)"
    # Link against FxPlug
    OTHER_LDFLAGS:
      - "-framework"
      - "FxPlug"
      - "$(inherited)"
    # Rpath so the XPC service can find FxPlug at runtime
    LD_RUNPATH_SEARCH_PATHS:
      - '"/Applications/Final Cut Pro.app/Contents/Frameworks"'
      - "$(inherited)"

# --- File groups ---
fileGroups:
  - Shared

# --- Targets ---
targets:

  # ============================================================
  # PHASE 1: BENDR VHS
  # ============================================================

  BENDRVHSApp:
    type: application
    platform: macOS
    sources:
      - path: BENDRVHSApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.vhs
        INFOPLIST_FILE: BENDRVHSApp/Info.plist
    dependencies:
      - target: BENDRVHSService
        embed: true
        codeSign: true
        copy:
          destination: plugins

  BENDRVHSService:
    type: xpc-service
    platform: macOS
    sources:
      - path: BENDRVHSService
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.vhs.service
        INFOPLIST_FILE: BENDRVHSService/Info.plist
        CODE_SIGN_ENTITLEMENTS: BENDRVHSService/BENDRVHSService.entitlements
        MTL_HEADER_SEARCH_PATHS:
          - "$(SRCROOT)/Shared/Metal"
          - "$(inherited)"

  # ============================================================
  # PHASE 1: BENDR CRT
  # ============================================================

  BENDRCRTApp:
    type: application
    platform: macOS
    sources:
      - path: BENDRCRTApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.crt
        INFOPLIST_FILE: BENDRCRTApp/Info.plist
    dependencies:
      - target: BENDRCRTService
        embed: true
        codeSign: true
        copy:
          destination: plugins

  BENDRCRTService:
    type: xpc-service
    platform: macOS
    sources:
      - path: BENDRCRTService
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.crt.service
        INFOPLIST_FILE: BENDRCRTService/Info.plist
        CODE_SIGN_ENTITLEMENTS: BENDRCRTService/BENDRCRTService.entitlements
        MTL_HEADER_SEARCH_PATHS:
          - "$(SRCROOT)/Shared/Metal"
          - "$(inherited)"

  # ============================================================
  # PHASE 1: BENDR Feedback
  # ============================================================

  BENDRFeedbackApp:
    type: application
    platform: macOS
    sources:
      - path: BENDRFeedbackApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.feedback
        INFOPLIST_FILE: BENDRFeedbackApp/Info.plist
    dependencies:
      - target: BENDRFeedbackService
        embed: true
        codeSign: true
        copy:
          destination: plugins

  BENDRFeedbackService:
    type: xpc-service
    platform: macOS
    sources:
      - path: BENDRFeedbackService
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.feedback.service
        INFOPLIST_FILE: BENDRFeedbackService/Info.plist
        CODE_SIGN_ENTITLEMENTS: BENDRFeedbackService/BENDRFeedbackService.entitlements
        MTL_HEADER_SEARCH_PATHS:
          - "$(SRCROOT)/Shared/Metal"
          - "$(inherited)"

  # ============================================================
  # PHASE 2: BENDR Colour
  # ============================================================

  BENDRColourApp:
    type: application
    platform: macOS
    sources:
      - path: BENDRColourApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.colour
        INFOPLIST_FILE: BENDRColourApp/Info.plist
    dependencies:
      - target: BENDRColourService
        embed: true
        codeSign: true
        copy:
          destination: plugins

  BENDRColourService:
    type: xpc-service
    platform: macOS
    sources:
      - path: BENDRColourService
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.colour.service
        INFOPLIST_FILE: BENDRColourService/Info.plist
        CODE_SIGN_ENTITLEMENTS: BENDRColourService/BENDRColourService.entitlements
        MTL_HEADER_SEARCH_PATHS:
          - "$(SRCROOT)/Shared/Metal"
          - "$(inherited)"

  # ============================================================
  # PHASE 2: BENDR Scan
  # ============================================================

  BENDRScanApp:
    type: application
    platform: macOS
    sources:
      - path: BENDRScanApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.scan
        INFOPLIST_FILE: BENDRScanApp/Info.plist
    dependencies:
      - target: BENDRScanService
        embed: true
        codeSign: true
        copy:
          destination: plugins

  BENDRScanService:
    type: xpc-service
    platform: macOS
    sources:
      - path: BENDRScanService
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.scan.service
        INFOPLIST_FILE: BENDRScanService/Info.plist
        CODE_SIGN_ENTITLEMENTS: BENDRScanService/BENDRScanService.entitlements
        MTL_HEADER_SEARCH_PATHS:
          - "$(SRCROOT)/Shared/Metal"
          - "$(inherited)"

  # ============================================================
  # PHASE 2: BENDR Corrupt
  # ============================================================

  BENDRCorruptApp:
    type: application
    platform: macOS
    sources:
      - path: BENDRCorruptApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.corrupt
        INFOPLIST_FILE: BENDRCorruptApp/Info.plist
    dependencies:
      - target: BENDRCorruptService
        embed: true
        codeSign: true
        copy:
          destination: plugins

  BENDRCorruptService:
    type: xpc-service
    platform: macOS
    sources:
      - path: BENDRCorruptService
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.corrupt.service
        INFOPLIST_FILE: BENDRCorruptService/Info.plist
        CODE_SIGN_ENTITLEMENTS: BENDRCorruptService/BENDRCorruptService.entitlements
        MTL_HEADER_SEARCH_PATHS:
          - "$(SRCROOT)/Shared/Metal"
          - "$(inherited)"

  # ============================================================
  # PHASE 3: BENDR Melt
  # ============================================================

  BENDRMeltApp:
    type: application
    platform: macOS
    sources:
      - path: BENDRMeltApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.melt
        INFOPLIST_FILE: BENDRMeltApp/Info.plist
    dependencies:
      - target: BENDRMeltService
        embed: true
        codeSign: true
        copy:
          destination: plugins

  BENDRMeltService:
    type: xpc-service
    platform: macOS
    sources:
      - path: BENDRMeltService
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.melt.service
        INFOPLIST_FILE: BENDRMeltService/Info.plist
        CODE_SIGN_ENTITLEMENTS: BENDRMeltService/BENDRMeltService.entitlements
        MTL_HEADER_SEARCH_PATHS:
          - "$(SRCROOT)/Shared/Metal"
          - "$(inherited)"

  # ============================================================
  # PHASE 3: BENDR Dirty
  # ============================================================

  BENDRDirtyApp:
    type: application
    platform: macOS
    sources:
      - path: BENDRDirtyApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.dirty
        INFOPLIST_FILE: BENDRDirtyApp/Info.plist
    dependencies:
      - target: BENDRDirtyService
        embed: true
        codeSign: true
        copy:
          destination: plugins

  BENDRDirtyService:
    type: xpc-service
    platform: macOS
    sources:
      - path: BENDRDirtyService
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.dirty.service
        INFOPLIST_FILE: BENDRDirtyService/Info.plist
        CODE_SIGN_ENTITLEMENTS: BENDRDirtyService/BENDRDirtyService.entitlements
        MTL_HEADER_SEARCH_PATHS:
          - "$(SRCROOT)/Shared/Metal"
          - "$(inherited)"

  # ============================================================
  # PHASE 3: BENDR Flow
  # ============================================================

  BENDRFlowApp:
    type: application
    platform: macOS
    sources:
      - path: BENDRFlowApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.flow
        INFOPLIST_FILE: BENDRFlowApp/Info.plist
    dependencies:
      - target: BENDRFlowService
        embed: true
        codeSign: true
        copy:
          destination: plugins

  BENDRFlowService:
    type: xpc-service
    platform: macOS
    sources:
      - path: BENDRFlowService
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.flow.service
        INFOPLIST_FILE: BENDRFlowService/Info.plist
        CODE_SIGN_ENTITLEMENTS: BENDRFlowService/BENDRFlowService.entitlements
        MTL_HEADER_SEARCH_PATHS:
          - "$(SRCROOT)/Shared/Metal"
          - "$(inherited)"

  # ============================================================
  # PHASE 3: BENDR Signal Lab
  # ============================================================

  BENDRSignalLabApp:
    type: application
    platform: macOS
    sources:
      - path: BENDRSignalLabApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.signallab
        INFOPLIST_FILE: BENDRSignalLabApp/Info.plist
    dependencies:
      - target: BENDRSignalLabService
        embed: true
        codeSign: true
        copy:
          destination: plugins

  BENDRSignalLabService:
    type: xpc-service
    platform: macOS
    sources:
      - path: BENDRSignalLabService
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.signallab.service
        INFOPLIST_FILE: BENDRSignalLabService/Info.plist
        CODE_SIGN_ENTITLEMENTS: BENDRSignalLabService/BENDRSignalLabService.entitlements
        MTL_HEADER_SEARCH_PATHS:
          - "$(SRCROOT)/Shared/Metal"
          - "$(inherited)"

  # ============================================================
  # PHASE 4: BENDR Synth
  # ============================================================

  BENDRSynthApp:
    type: application
    platform: macOS
    sources:
      - path: BENDRSynthApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.synth
        INFOPLIST_FILE: BENDRSynthApp/Info.plist
    dependencies:
      - target: BENDRSynthService
        embed: true
        codeSign: true
        copy:
          destination: plugins

  BENDRSynthService:
    type: xpc-service
    platform: macOS
    sources:
      - path: BENDRSynthService
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.synth.service
        INFOPLIST_FILE: BENDRSynthService/Info.plist
        CODE_SIGN_ENTITLEMENTS: BENDRSynthService/BENDRSynthService.entitlements
        MTL_HEADER_SEARCH_PATHS:
          - "$(SRCROOT)/Shared/Metal"
          - "$(inherited)"

  # ============================================================
  # PHASE 4: BENDR Transition
  # ============================================================

  BENDRTransitionApp:
    type: application
    platform: macOS
    sources:
      - path: BENDRTransitionApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.transition
        INFOPLIST_FILE: BENDRTransitionApp/Info.plist
    dependencies:
      - target: BENDRTransitionService
        embed: true
        codeSign: true
        copy:
          destination: plugins

  BENDRTransitionService:
    type: xpc-service
    platform: macOS
    sources:
      - path: BENDRTransitionService
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.transition.service
        INFOPLIST_FILE: BENDRTransitionService/Info.plist
        CODE_SIGN_ENTITLEMENTS: BENDRTransitionService/BENDRTransitionService.entitlements
        MTL_HEADER_SEARCH_PATHS:
          - "$(SRCROOT)/Shared/Metal"
          - "$(inherited)"

  # ============================================================
  # PHASE 4: BENDR Spatial
  # ============================================================

  BENDRSpatialApp:
    type: application
    platform: macOS
    sources:
      - path: BENDRSpatialApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.spatial
        INFOPLIST_FILE: BENDRSpatialApp/Info.plist
    dependencies:
      - target: BENDRSpatialService
        embed: true
        codeSign: true
        copy:
          destination: plugins

  BENDRSpatialService:
    type: xpc-service
    platform: macOS
    sources:
      - path: BENDRSpatialService
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.spatial.service
        INFOPLIST_FILE: BENDRSpatialService/Info.plist
        CODE_SIGN_ENTITLEMENTS: BENDRSpatialService/BENDRSpatialService.entitlements
        MTL_HEADER_SEARCH_PATHS:
          - "$(SRCROOT)/Shared/Metal"
          - "$(inherited)"

  # ============================================================
  # PHASE 4: BENDR Optics
  # ============================================================

  BENDROpticsApp:
    type: application
    platform: macOS
    sources:
      - path: BENDROpticsApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.optics
        INFOPLIST_FILE: BENDROpticsApp/Info.plist
    dependencies:
      - target: BENDROpticsService
        embed: true
        codeSign: true
        copy:
          destination: plugins

  BENDROpticsService:
    type: xpc-service
    platform: macOS
    sources:
      - path: BENDROpticsService
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.bendr.fxplug.optics.service
        INFOPLIST_FILE: BENDROpticsService/Info.plist
        CODE_SIGN_ENTITLEMENTS: BENDROpticsService/BENDROpticsService.entitlements
        MTL_HEADER_SEARCH_PATHS:
          - "$(SRCROOT)/Shared/Metal"
          - "$(inherited)"

schemes:
  BENDRVHSApp:
    build:
      targets:
        BENDRVHSApp: all
    run:
      config: Debug

  BENDRCRTApp:
    build:
      targets:
        BENDRCRTApp: all
    run:
      config: Debug

  BENDRFeedbackApp:
    build:
      targets:
        BENDRFeedbackApp: all
    run:
      config: Debug

  BENDRColourApp:
    build:
      targets:
        BENDRColourApp: all
    run:
      config: Debug

  BENDRScanApp:
    build:
      targets:
        BENDRScanApp: all
    run:
      config: Debug

  BENDRCorruptApp:
    build:
      targets:
        BENDRCorruptApp: all
    run:
      config: Debug

  BENDRMeltApp:
    build:
      targets:
        BENDRMeltApp: all
    run:
      config: Debug

  BENDRDirtyApp:
    build:
      targets:
        BENDRDirtyApp: all
    run:
      config: Debug

  BENDRFlowApp:
    build:
      targets:
        BENDRFlowApp: all
    run:
      config: Debug

  BENDRSignalLabApp:
    build:
      targets:
        BENDRSignalLabApp: all
    run:
      config: Debug

  BENDRSynthApp:
    build:
      targets:
        BENDRSynthApp: all
    run:
      config: Debug

  BENDRTransitionApp:
    build:
      targets:
        BENDRTransitionApp: all
    run:
      config: Debug

  BENDRSpatialApp:
    build:
      targets:
        BENDRSpatialApp: all
    run:
      config: Debug

  BENDROpticsApp:
    build:
      targets:
        BENDROpticsApp: all
    run:
      config: Debug

  AllPlugins:
    build:
      targets:
        BENDRVHSApp: all
        BENDRCRTApp: all
        BENDRFeedbackApp: all
        BENDRColourApp: all
        BENDRScanApp: all
        BENDRCorruptApp: all
        BENDRMeltApp: all
        BENDRDirtyApp: all
        BENDRFlowApp: all
        BENDRSignalLabApp: all
        BENDRSynthApp: all
        BENDRTransitionApp: all
        BENDRSpatialApp: all
        BENDROpticsApp: all
    run:
      config: Debug
